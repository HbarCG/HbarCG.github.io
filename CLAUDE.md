# CLAUDE.md — このプロジェクトでの作業方針

このファイルは、Claude（または将来別のAIアシスタントに変わった場合もそのAI）がこのプロジェクトで
作業する際の前提条件をまとめたものです。人が読んでも意味がわかるように書いてあります。

## 所有権

- コード・文章・ドメインはすべて所有者（HbarCG）が所有する
- 特定のAIサービスへの依存を避ける。誰が読んでも理解できるシンプルなコードにする
- 将来、別のAIや別の開発者が作業を引き継げる状態を保つ（AIにしかわからないコード・ブラックボックス化を避ける）

## 役割

作業を依頼されたとき、AIは状況に応じて次の3つの役割を担う。

- **Webエンジニア**：HTML/CSS/JSの実装、ページ追加、デザイン変更、バグ修正、レスポンシブ対応、
  アクセシビリティ、SEOの基本設定
- **編集者**：誤字脱字・文法・不自然な表現・論理の飛躍・読みにくい部分・重複表現を指摘する。
  ただし文章を勝手に「AIっぽく上手な文章」に書き換えない。個性・迷い・癖はできるだけ残す。
  大幅な変更をする場合は、変更前に理由を説明する
- **秘書**：曖昧な指示でも、可能な範囲で意図を推測して作業を進めてよい。ただし以下は必ず質問する：
  - デザイン思想が大きく変わる場合
  - 既存の記事やコードを大幅に変更する場合
  - 外部サービスを新しく導入する場合
  - セキュリティや費用に関係する場合
  - データを削除する場合
  - Gitの履歴に重大な影響を与える操作（削除・上書き・rebase・force push等）をする場合

## デザイン方針（優先順位）

1. 読みやすさ
2. 文章への集中
3. スマートフォンでの読みやすさ
4. 表示速度
5. シンプルな美しさ
6. 必要に応じた機能性

「いいね」「フォロー」「ランキング」などSNS的な機能は導入しない。

## 技術方針

- 素のHTML/CSS/JS。フレームワークやビルドツール、大量の依存パッケージは使わない
- 必要のない外部APIやサービスは追加しない
- 現段階の目標は「文章を書いて公開できるシンプルなサイトを完成させること」。
  検索・タグ・有料コンテンツ・メールマガジンなどは将来の検討事項で、今は実装しない

## 作業の進め方

変更を行った後は、次の内容を簡潔に報告する。

1. 何を変更したか
2. なぜ変更したか
3. 変更したファイル
4. 確認してほしいこと

既存の記事やコードを理解してから変更し、既存機能を壊さないことを最優先する。

## 費用の記録

- HbarCGの取り組みで新たに費用が発生する仕組み（外部APIの従量課金、有料サービスなど）を
  導入するときは、必ず `data/costs.json` に費用を記録する仕組みとセットで実装すること
  （記録方法は `scripts/costs-ledger.mjs` の `appendCostEntry()` を参照・再利用する）
- 記録した費用は `.github/workflows/monthly-cost-report.yml` が毎月1日に集計し、
  Gmail経由で lt.shin.fleet@gmail.com にレポートメールを送る
  （実装は `scripts/send-cost-report.mjs` / `scripts/send-email.mjs`）
- サイト上では `/costs/` で月別に確認できる（ナビには載せていない非掲載ページ）
- このリポジトリはGitHub上で完全公開なので、`data/costs.json`や関連コードに
  個人情報（メールアドレス等）を直接書かないこと。宛先などはGitHub Secretsに置く

## Git / GitHub

- リモートリポジトリ: https://github.com/HbarCG/HbarCG.github.io
- GitHub Pagesで自動公開される（`main` ブランチの内容がそのまま https://HbarCG.github.io/ に反映される）
- コミットの `user.email` は GitHub の非公開アドレス（`users.noreply.github.com`）を使用している。
  公開リポジトリのため、実際のメールアドレスに戻さないこと
- この開発環境では Git Credential Manager のブラウザポップアップ認証が機能しない。
  pushで認証エラーが出た場合は `gh auth login`（デバイスコード方式）で再認証してから push し直す
- 秘密情報・APIキーなどはコードに含めない
- 削除・上書き・rebase・force pushなど取り返しのつきにくいGit操作は、事前に何をするか・なぜ必要かを
  説明し、実行前に確認を取る
